/**:
    ros__parameters:
        server:
            host: '0.0.0.0'
            port: 8080
        cors:
            allowed_origins:
                - 'http://localhost:5173'
        discovery:
            mode: 'manifest_only'
            manifest_path: '/e2e/manifest.yaml'
        scripts:
            scripts_dir: '/e2e-uploads'
            allow_uploads: true
